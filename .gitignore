# Runtime state files - don't commit
current.json
sessions_detail.json
token_stats.json
*.log
.lock/

# Backups
backups/
*.bak
*.bak_*

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp
*~

# Personal credentials / secrets (never commit)
_TENCENT_PASSWORD.txt
*.password
*.secret
.env

# VAPID keys + push subscriptions (per-user, generated by scripts/generate-vapid.sh)
vapid_private.pem
vapid_public.pem
vapid_keys.json
subscriptions.json

# Python venv (created during install: python3 -m venv .venv)
.venv/
